[package]
name = "rust-agent"
version = "0.1.0"
edition = "2024"
build = "build.rs"

[dependencies]
log = "0.4"
env_logger = "0.11"
chrono = { version = "0.4", default-features = false, features = ["std", "clock"] }
prost = "0.12"
zmq = "0.10"
ctrlc = "3"

[build-dependencies]
prost-build = "0.12"
